# Инструмент: bin/cortex

Один файл, стандартная библиотека Python, ноль внешних зависимостей. Ставить
нечего, поднимать нечего, форк движка ничему не мешает.

Markdown в git — носитель истины. Индекс и граф строятся в памяти при каждом
запуске (полтысячи узлов ≈ 0.4 c) и **не хранятся**: производный артефакт не
бывает истиной, а хранимый индекс неизбежно расходится с файлами.

## Команды

| Команда | Что делает | Пишет в vault |
|---|---|---|
| `doctor` | что загружено, какой схемой проверяется, чего не хватает | нет |
| `gate` | проверка памяти по схеме; `--all`, `--json` | нет |
| `refresh` | пересчёт `ripeness` и `effective_cp`; `--dry-run` | **да** |
| `report` | узлы, зоны, средняя ступень, увядание, спелость; `--json` | нет |
| `ask "<тема>"` | что память знает, с якорями; `--scope`, `--nexus`, `--json` | нет |
| `context` | `--node` / `--kr` / `--scope`, JSON для агента | нет |
| `who "<тема>"` | кому писать: владельцы релевантных узлов | нет |
| `stale` | очередь верификации протухшего; `--soon` | нет |
| `gaps` | знание, не достижимое от хребта целей | нет |
| `agents` | агенты, их зоны записи и что кто записал | нет |
| `intake list\|route` | очередь неразобранного и куда оно ложится | нет |
| `node new\|set\|check` | контракт записи; всегда сначала `--dry-run` | **да** |
| `nexus list\|add` | объекты управления | **да** |
| `schema show\|init` | схема памяти: показать или сделать своей | **да** |
| `init` | привязать vault к организации | **да** |

Работа с чужим vault: `bin/cortex --root /путь/к/vault <команда>` или
переменная `CORTEX_ROOT`.

## Схема как данные

`GROUND/SCHEMA/` — не конфигурация инструмента, а модель памяти:

| Файл | Что задаёт |
|---|---|
| `nodes.yaml` | обязательные ключи, enum-значения, каталог типов узлов, требования по слою |
| `edges.yaml` | какое поле frontmatter даёт какое ребро и между чем |
| `profiles.yaml` | `change_rate` → срок годности; скорость распада уверенности |
| `ladder.yaml` | лестница доказательств 2–9, потолки канала и полномочий, правила |
| `agents.yaml` | классы агентов, их зоны записи, что не делегируется никому |
| `checks.yaml` | жёсткость каждой проверки памяти: `error` · `warn` · `off` |
| `gates.yaml` | гейты методологии: при каком CP и к какой дате обещание уходит наружу |

Инструмент читает их при каждом запуске. **Новый тип узла, новый объект
управления, новый агент — это декларация, а не правка кода и не PR в чужой
репозиторий.** Отсюда следует совместимость: Кортекс работает на уже наполненном
vault, а если в нём нет своей схемы — берёт ту, с которой поставлен.

## Как считается уверенность

```
cp              ступень 2..9, считанная с лестницы по сильнейшему артефакту
confidence      то же число в долях: cp / 9 — для совместимости и отчётов
effective_cp    cp − распад(время × change_rate)
```

Распад: `p = (сегодня − updated) / ttl_days`. При `p < 0.5` узел свежий,
`< 1.0` созревающий, `≥ 1.0` увядающий. За каждый просроченный срок годности
узел теряет `decay.steps_per_ttl` ступеней, но не ниже `decay.floor`.

Смысл в том, что **протухшее знание теряет уверенность само**, без ручного
прохода по памяти. Продлить дату без проверки источника нельзя — это подделка
свежести, и гейт показывает такой дрейф отдельно.

## Поиск без графовой БД

`ask` строит частотный индекс по заголовкам, тегам и телу узлов, ранжирует
по совпадению и корректирует вес: уверенное знание выигрывает у слухов,
протухшее уступает свежему. `context` обходит рёбра из `edges.yaml` в обе
стороны на заданную глубину.

Этого достаточно для памяти масштаба организации. Векторный поиск и граф-стор
— оптимизация, которая имеет смысл на порядок большем объёме; принимать ради
неё зависимость от сервиса на старте не нужно.

## Два теста и что каждый доказывает

| Тест | Отвечает на вопрос | Когда красный |
|---|---|---|
| Кортекс — дымовой тест: /home/user/poh-cortex
  ok   doctor отвечает
  ok   схема читается
  ok   гейт проходит
  ok   свежесть пересчитана
  ok   отчёт собирается
  ok   поиск отвечает
  ok   очередь читается
  ok   зоны агентов видны
  ok   сироты считаются
  ok   узел без источника отклонён
  ok   чужой Нексус отклонён
  ok   висячее ребро отклонено
  ok   неизвестный тип отклонён
  ok   профили перечислены
  ok   очередь приёмки отвечает
  ok   неизвестный профиль отклонён
  ok   срез без профиля отклонён
  ok   срезы всех профилей
  ok   профиль не пишет в ядро
  ok   профиль не пишет в чужую зону
  ok   стыки считаются
  ok   --as после подкоманды
  ok   срез профиля собирается
  ok   повестка дня собирается
  ok   расхождения ищутся
  ok   калибровка считается
  ok   проверка узла отвечает
  ok   рост CP без источника отклонён
  ok   снятие спора без источника
  ok   резолвер находит узел по имени

✓ всё зелёное: 30 проверок | команды не падают, контракты отказа на месте | сломан инструмент |
| Приёмка Кортекса
  шаблон: /home/user/poh-cortex

  профиль: po-example-a · зона: team:a

Что Кортекс отказывается делать
  ✓ утверждение без источника не записывается
  ✓ уверенность не поднимается рассуждением
  ✓ профиль не пишет в ядро организации
  ✓ Нексус, не объявленный в реестре, не принимает записи

Что Кортекс делает
  ✓ та же ступень с артефактом — принимается
  ✓ протухшее знание теряет ступень само: cp 6 → effective_cp 2, wilting
  ✓ протухшее попало в очередь верификации

Что Кортекс считает за вас
  ✓ ставка с CP 2 на зависимостях 3 · 6 · 2 — гейт зелёный
  ✓ обещание выше слабого звена остановлено: CP 7 выше слабого звена: 'vozvraty-i-otmeny' стоит на 2

Ради чего всё: на чём стоит цель
  цель  «Подключены 3 организатора»  CP 9   ← утверждена
  стоит на  bet-zapusk  CP 2   ← чем подтверждена

  Разрыв между этими двумя строками виден в начале квартала, а не в конце.

✓ приёмка пройдена: 9 проверок, пропущено 0 | гарантии памяти держат | сломано правило: источник, ступень, распад, зона, слабое звено |

Приёмка собирает одноразовый vault из шаблона и не трогает вашу память.
Проверяет причину отказа, а не код возврата; падение инструмента отказом
не считается.

## Что именно проверяет гейт

| Проверка | По умолчанию | Что ловит |
|---|---|---|
| `registry_drift` | error | папка Нексуса с узлами, не объявленная в реестре |
| `unknown_node_type` | error | тип узла, не объявленный ни в схеме, ни в реестре |
| `weakest_link_drift` | error | CP ставки выше слабейшей из её зависимостей; опечатка в `non_critical` |
| `ripeness_drift` | warn | вычисляемое поле разошлось — нужен `cortex refresh` |
| `unknown_agent` | warn | `captured_by` вне `SCHEMA/agents.yaml` |
| `missing_provenance` | warn | узел без `captured_by`: не видно, кто записал |
| `agent_zone_drift` | warn | зона агента ссылается на несуществующий Нексус |

Плюс безусловные: узел без `sources`, ссылка на несуществующий `node_id`,
запись мимо зоны профиля, дубликат `node_id`.

## Команды методички и навыки этого репозитория

Гайд PAF Product Ops называет команды агента по-своему. Здесь они собраны иначе —
под задачу, а не под шаг настройки. Соответствие:

| В методичке | Здесь | Чем закрыто |
|---|---|---|
| `/paf-profile` | `cortex init` | организация, продукт, продуктовый инженер |
| `/paf-rules` | `ONBOARDING.md`, разделы Г · Д · Ж | лестница, распад, гейты — три интервью |
| `/paf-bet` | `/cortex-bet` | декомпозиция цели на ставки |
| `/paf-cp` | `/cortex-bet`, раздел «Посчитать уверенность» | базовый замер по артефактам |
| `/paf-evidence` | `/cortex-intake` | разбор `inbox.md` в артефакты и узлы |
| — | `/cortex-status` | отчёт руководителю из ставок |
| — | `/cortex-ask` | ответ по памяти с якорями |
| — | `/paf-gate` | проверка целостности памяти |

Имена разошлись сознательно: `paf-*` остались за настройкой и схемой,
`cortex-*` — за ежедневной работой на уже настроенной памяти.
